---
layout: json
---

[ {% for contract in site.data.contracts %}{
  "name" : "{{ contract.name }}",
  "version" : "{{ contract.version }}",
  "javaSELevel" : {{ contract.javaSELevel | jsonify }},
  "javaEELevel" : {{ contract.javaEELevel | jsonify }},
  "packages" : {{ contract.packages | jsonify }},
  "comments" : "{{ contract.comments }}",
  "jsr" : "{{ contract.jsr }}"
}{% if forloop.last == false %},{% endif %} {% endfor %} ]
